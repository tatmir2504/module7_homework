const obj = {
  subject: 'serial',
  name: 'The_Seven',
  series: 20}
  
  function createObj(obj) {
	let staples = {};
	
	return obj = staples;
  }

  let object = createObj('obj')
  console.log((object));
